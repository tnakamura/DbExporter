# DbExporter

Simple database export tool.

## USAGE

```
DbExporter.exe \
  -s localhost \
  -d northwind \
  -u test_user \
  -p test_pass \
  -f csv \
  -o ".\sample.csv" \
  -q "SELECT * FROM samples"
```

